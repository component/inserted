{
  "name": "inserted-component",
  "repo": "component/inserted",
  "description": "invoke a callback when a DOM element is inserted",
  "version": "0.0.1",
  "keywords": ["dom", "event"],
  "dependencies": {},
  "development": {},
  "component": {
    "styles": [
      "inserted.css"
    ],
    "scripts": {
      "inserted/index.js": "index.js"
    }
  }
}